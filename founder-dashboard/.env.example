# RecepAI Founder Dashboard — Push token
# Copiază acest fișier la .env și pune token-ul tău acolo:
#   cp .env.example .env
#
# .env NU se commitiează (e în .gitignore).
# Tokenul trebuie să fie fine-grained, doar pe repo + Contents: Read and write.

GITHUB_TOKEN=github_pat_11...aici_tokenul_tau
